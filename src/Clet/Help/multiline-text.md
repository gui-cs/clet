## Examples

```sh
# Multi-line text editor:
clet multiline-text

# Short alias:
clet mt --title "Enter a commit message"

# Pre-filled content:
clet mt --initial "First line\nSecond line"

# JSON output:
clet mt --json
# → {"schemaVersion":1,"status":"ok","value":"line 1\nline 2\nline 3"}
```
